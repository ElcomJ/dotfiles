sudo update-alternatives --config java

sudo apt install openjdk-8-jdk
sudo apt install default-jre

Depois disso, é só configurar a versão que vai utilizar em etc/environment.

Depois dar reboot no pc e testar echo $JAVA_HOME
